<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <!-- PERMISOS DE LA APLICACIÓN -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- Acceso a Internet-->
    <uses-feature android:name="android.hardware.camera" android:required="false" />  <!-- Característica de Cámara (no obligatoria) -->
    <uses-permission android:name="android.permission.CAMERA" />   <!-- Permiso USO de Cámara -->

    <!-- CONFIGURACIÓN DE LA APLICACIÓN -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RecetarioApp" >

        <!-- ACTIVIDADES -->

        <!-- SplashActivity - Pantalla de presentación/inicio -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.RecetarioApp.Splash">
            <intent-filter>
                <!-- Actividad principal que se lanza al abrir la app -->
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LoginActivity - Pantalla de inicio de sesión -->
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- RegisterActivity - Pantalla de registro de usuario -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- MainActivity - Pantalla principal después del login -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- RecipeDetailActivity - Pantalla de detalle de recetas -->
        <activity
            android:name=".ui.RecipeDetailActivity"
            android:theme="@style/Theme.RecetarioApp"
            android:parentActivityName=".MainActivity" />

        <!-- FILEPROVIDER PARA COMPARTIR ARCHIVOS -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>