<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema principal de la app - Estilo Recetario Clásico -->
    <style name="Theme.RecetarioClasico" parent="Theme.Material3.Light.NoActionBar">
        <!-- Colores principales -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryVariant">@color/color_primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>

        <item name="colorSecondary">@color/color_secondary</item>
        <item name="colorSecondaryVariant">@color/color_primary_light</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Colores de superficie -->
        <item name="colorSurface">@color/color_background</item>
        <item name="colorOnSurface">@color/color_text_primary</item>
        <item name="android:colorBackground">@color/color_background_light</item>

        <!-- Color de error -->
        <item name="colorError">@color/color_error</item>
        <item name="colorOnError">@color/white</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@color/color_primary</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Navigation bar -->
        <item name="android:navigationBarColor">@color/color_background</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">true</item>

        <!-- Tipografía -->
        <item name="fontFamily">@font/raleway_regular</item>
        <item name="android:fontFamily">@font/raleway_regular</item>

        <!-- Formas -->
        <item name="shapeAppearanceSmallComponent">@style/ShapeAppearance.RecetarioClasico.SmallComponent</item>
        <item name="shapeAppearanceMediumComponent">@style/ShapeAppearance.RecetarioClasico.MediumComponent</item>
        <item name="shapeAppearanceLargeComponent">@style/ShapeAppearance.RecetarioClasico.LargeComponent</item>
    </style>

    <!-- Formas personalizadas -->
    <style name="ShapeAppearance.RecetarioClasico.SmallComponent" parent="ShapeAppearance.Material3.SmallComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">4dp</item>
    </style>

    <style name="ShapeAppearance.RecetarioClasico.MediumComponent" parent="ShapeAppearance.Material3.MediumComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">8dp</item>
    </style>

    <style name="ShapeAppearance.RecetarioClasico.LargeComponent" parent="ShapeAppearance.Material3.LargeComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">12dp</item>
    </style>

    <!-- Toolbar personalizado -->
    <style name="Widget.RecetarioClasico.Toolbar" parent="Widget.Material3.Toolbar">
        <item name="titleTextColor">@color/white</item>
        <item name="subtitleTextColor">@color/color_secondary_light</item>
        <item name="android:background">@color/color_primary</item>
    </style>

    <!-- Botones -->
    <style name="Widget.RecetarioClasico.Button" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/color_primary</item>
        <item name="cornerRadius">4dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:fontFamily">@font/raleway_semibold</item>
    </style>

    <style name="Widget.RecetarioClasico.Button.Outlined" parent="Widget.Material3.Button.OutlinedButton">
        <item name="strokeColor">@color/color_primary</item>
        <item name="android:textColor">@color/color_primary</item>
        <item name="cornerRadius">4dp</item>
    </style>

    <!-- Cards -->
    <style name="Widget.RecetarioClasico.CardView" parent="Widget.Material3.CardView.Elevated">
        <item name="cardBackgroundColor">@color/color_background</item>
        <item name="cardElevation">3dp</item>
        <item name="cardCornerRadius">8dp</item>
    </style>

    <!-- TextInputLayout -->
    <style name="Widget.RecetarioClasico.TextInputLayout" parent="Widget.Material3.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">@color/color_primary</item>
        <item name="hintTextColor">@color/color_text_hint</item>
        <item name="android:textColorHint">@color/color_text_hint</item>
    </style>
</resources>