<?xml version="1.0" encoding="utf-8"?>
<!-- Archivo: item_receta.xml - Elemento de lista para recetas -->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="12dp"
    android:background="@drawable/bg_card"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <!-- Tarjeta para la imagen de la receta -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/card_imagen"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            app:cardCornerRadius="16dp"
            app:cardElevation="0dp"
            app:strokeWidth="0dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <!-- Imagen de la receta -->
            <ImageView
                android:id="@+id/iv_receta_imagen"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:contentDescription="@string/sin_imagen"
                android:background="@color/color_surface"
                tools:src="@android:drawable/ic_menu_gallery" />

        </com.google.android.material.card.MaterialCardView>

        <!-- Contenedor del botón de favorito -->
        <FrameLayout
            android:id="@+id/btn_favorito_container"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_margin="12dp"
            android:background="@drawable/bg_cuadrado"
            android:clickable="true"
            android:focusable="true"
            app:layout_constraintEnd_toEndOf="@id/card_imagen"
            app:layout_constraintTop_toTopOf="@id/card_imagen">

            <!-- Icono de favorito -->
            <ImageView
                android:id="@+id/btn_favorito"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_fav_no"
                android:contentDescription="@string/nav_favs"
                app:tint="@color/color_accent" />

        </FrameLayout>

        <!-- Contenido informativo de la receta -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="12dp"
            app:layout_constraintTop_toBottomOf="@id/card_imagen"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <!-- Nombre de la receta -->
            <TextView
                android:id="@+id/tv_receta_nombre"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="18sp"
                android:textStyle="bold"
                android:textColor="@color/color_primary"
                android:fontFamily="serif"
                android:maxLines="2"
                android:ellipsize="end"
                tools:text="Carbonara original" />

            <!-- Descripción breve de la receta -->
            <TextView
                android:id="@+id/tv_receta_descripcion"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:textSize="14sp"
                android:textColor="@color/color_text_secondary"
                android:maxLines="3"
                android:ellipsize="end"
                tools:text="La verdadera receta de carbonara..." />

            <!-- Metadatos de la receta: tiempo, dificultad y porciones -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:orientation="horizontal">

                <!-- Tiempo de preparación -->
                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="horizontal"
                    android:gravity="center_vertical">

                    <ImageView
                        android:layout_width="16dp"
                        android:layout_height="16dp"
                        android:src="@android:drawable/ic_menu_recent_history"
                        app:tint="@color/color_text_hint"
                        android:contentDescription="Tiempo" />

                    <TextView
                        android:id="@+id/tv_receta_tiempo"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="4dp"
                        android:textSize="12sp"
                        android:textColor="@color/color_text_secondary"
                        tools:text="20 min" />

                </LinearLayout>

                <!-- Nivel de dificultad -->
                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="horizontal"
                    android:gravity="center_vertical">

                    <ImageView
                        android:layout_width="16dp"
                        android:layout_height="16dp"
                        android:src="@android:drawable/ic_menu_compass"
                        app:tint="@color/color_text_hint"
                        android:contentDescription="Dificultad" />

                    <TextView
                        android:id="@+id/tv_receta_dificultad"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="4dp"
                        android:textSize="12sp"
                        android:textColor="@color/color_text_secondary"
                        tools:text="Media" />

                </LinearLayout>

                <!-- Número de porciones -->
                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="horizontal"
                    android:gravity="center_vertical">

                    <ImageView
                        android:layout_width="16dp"
                        android:layout_height="16dp"
                        android:src="@android:drawable/ic_menu_agenda"
                        app:tint="@color/color_text_hint"
                        android:contentDescription="Porciones" />

                    <TextView
                        android:id="@+id/tv_receta_porciones"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="4dp"
                        android:textSize="12sp"
                        android:textColor="@color/color_text_secondary"
                        tools:text="4 porc." />

                </LinearLayout>

            </LinearLayout>

            <!-- Chip para mostrar la categoría de la receta -->
            <com.google.android.material.chip.Chip
                android:id="@+id/chip_categoria"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                app:chipBackgroundColor="@color/color_secondary_light"
                app:chipStrokeWidth="0dp"
                android:textColor="@color/color_text_primary"
                android:textSize="11sp"
                tools:text="Platos Principales" />

        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</FrameLayout>