<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardBackgroundColor="@color/color_background"
    app:cardCornerRadius="8dp"
    app:cardElevation="3dp"
    app:strokeWidth="1dp"
    app:strokeColor="@color/color_border">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Imagen de la receta -->
        <ImageView
            android:id="@+id/iv_receta_imagen"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:scaleType="centerCrop"
            android:contentDescription="@string/sin_imagen"
            android:background="@color/color_surface"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            tools:src="@android:drawable/ic_menu_gallery" />

        <!-- Overlay para el botón de favorito -->
        <ImageButton
            android:id="@+id/btn_favorito"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_margin="8dp"
            android:background="@android:drawable/screen_background_light_transparent"
            android:contentDescription="@string/nav_favs"
            android:src="@android:drawable/star_big_off"
            app:layout_constraintEnd_toEndOf="@id/iv_receta_imagen"
            app:layout_constraintTop_toTopOf="@id/iv_receta_imagen"
            app:tint="@color/white" />

        <!-- Contenido de la card -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp"
            app:layout_constraintTop_toBottomOf="@id/iv_receta_imagen"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <!-- Nombre de la receta -->
            <TextView
                android:id="@+id/tv_receta_nombre"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="18sp"
                android:textStyle="bold"
                android:textColor="@color/color_primary"
                android:fontFamily="serif"
                android:maxLines="2"
                android:ellipsize="end"
                tools:text="Arroz con Chalotas y Setas" />

            <!-- Descripción -->
            <TextView
                android:id="@+id/tv_receta_descripcion"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:textSize="14sp"
                android:textColor="@color/color_text_secondary"
                android:maxLines="3"
                android:ellipsize="end"
                tools:text="Un arroz reconfortante con pollo, chalotas y setas shimeji, realzado con azafrán..." />

            <!-- Metadata: tiempo, dificultad, porciones -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:orientation="horizontal">

                <!-- Tiempo -->
                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="horizontal"
                    android:gravity="center_vertical">

                    <ImageView
                        android:layout_width="16dp"
                        android:layout_height="16dp"
                        android:src="@android:drawable/ic_menu_recent_history"
                        app:tint="@color/color_text_hint"
                        android:contentDescription="Tiempo" />

                    <TextView
                        android:id="@+id/tv_receta_tiempo"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="4dp"
                        android:textSize="12sp"
                        android:textColor="@color/color_text_secondary"
                        tools:text="60 min" />

                </LinearLayout>

                <!-- Dificultad -->
                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="horizontal"
                    android:gravity="center_vertical">

                    <ImageView
                        android:layout_width="16dp"
                        android:layout_height="16dp"
                        android:src="@android:drawable/ic_menu_compass"
                        app:tint="@color/color_text_hint"
                        android:contentDescription="Dificultad" />

                    <TextView
                        android:id="@+id/tv_receta_dificultad"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="4dp"
                        android:textSize="12sp"
                        android:textColor="@color/color_text_secondary"
                        tools:text="Media" />

                </LinearLayout>

                <!-- Porciones -->
                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="horizontal"
                    android:gravity="center_vertical">

                    <ImageView
                        android:layout_width="16dp"
                        android:layout_height="16dp"
                        android:src="@android:drawable/ic_menu_agenda"
                        app:tint="@color/color_text_hint"
                        android:contentDescription="Porciones" />

                    <TextView
                        android:id="@+id/tv_receta_porciones"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="4dp"
                        android:textSize="12sp"
                        android:textColor="@color/color_text_secondary"
                        tools:text="4 porc." />

                </LinearLayout>

            </LinearLayout>

            <!-- Categoría (chip) -->
            <com.google.android.material.chip.Chip
                android:id="@+id/chip_categoria"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                app:chipBackgroundColor="@color/color_secondary_light"
                app:chipStrokeWidth="0dp"
                android:textColor="@color/color_text_primary"
                android:textSize="11sp"
                tools:text="Platos Principales" />

        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>